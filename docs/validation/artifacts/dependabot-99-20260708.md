# Dependabot alert #99 (pytest) — Track B closure

<!-- Assisted by: cursor, claude -->

| Field         | Value                                                                |
| ------------- | -------------------------------------------------------------------- |
| Date          | 2026-07-08                                                           |
| Repo          | Contributor fork (`origin`), not upstream                            |
| Alert         | Security / Dependabot **#99** (not a GitHub issue)                   |
| Package       | `pytest` (dev dependency)                                            |
| Pin on `main` | `requirements-dev.txt` L4: `pytest==9.0.3` (`main` @ `b031b79`)      |
| Outcome       | **UI-only / blocked-on-UI** — code already fixed; API cannot dismiss |

## API triage (blocked)

PAT `~/.github/12312026_caisat_token.txt` (fine-grained) can read the repo but **cannot** access Dependabot alerts:

```text
GET/PATCH .../repos/<fork-owner>/CAIsat/dependabot/alerts/99
→ HTTP 403 Resource not accessible by personal access token
```

Same 403 for list and dismiss on both fork and upstream. No Dependabot fix PR is required: `pytest==9.0.3` is already on `main`.

No open Dependabot PR for pytest was found via `gh pr list` / `gh issue view 99` (alert #99 is a Security tab ID, not a GitHub issue).

## Manual dismiss (fork Security UI)

Do this once in a browser session with write access to the fork:

1. Open the fork on GitHub → **Security** → **Dependabot alerts**
   (URL shape: `https://github.com/<fork-owner>/CAIsat/security/dependabot`).
2. Open alert **#99** (pytest).
3. Confirm the alert targets an older pytest than `9.0.3` (or is already satisfied by the current pin).
4. Choose **Dismiss alert** → reason **A fix has already been started** (or the UI's "fixed" / "fix started" equivalent).
5. Optional comment: `pytest==9.0.3 already on main (requirements-dev.txt); Track B 2026-07-08`.
6. Confirm the alert no longer appears under Open.

If the UI still shows a remaining vulnerability on `9.0.3`, open a Dependabot/version bump PR instead of dismissing — that path was not indicated by the current pin.

## Handover

- Item "Dependabot #99" → **verified fixed in tree**; dismiss remains a one-click UI action (PAT lacks Dependabot alerts / security-alerts permission).
- No code change; no fix PR.

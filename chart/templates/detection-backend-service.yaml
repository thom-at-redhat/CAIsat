apiVersion: v1
kind: Service
metadata:
  name: {{ include "zoom-enhance.fullname" . }}-detection-backend
  labels:
    {{- include "zoom-enhance.labels" . | nindent 4 }}
    app.kubernetes.io/component: detection-backend
spec:
  type: {{ .Values.detectionBackend.service.type }}
  ports:
  - port: {{ .Values.detectionBackend.service.port }}
    targetPort: http
    protocol: TCP
    name: http
  selector:
    {{- include "zoom-enhance.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: detection-backend

# CAIsat Python tooling configuration
# Assisted by: cursor, claude

[tool.ruff]
line-length = 200

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["backend", "backend-detection"]

[[tool.mypy.overrides]]
module = ["numpy.*"]
ignore_missing_imports = true
